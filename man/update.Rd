% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{update}
\alias{update}
\title{Update out-of-date hvtiR members}
\usage{
update(force = FALSE)
}
\arguments{
\item{force}{Bypass the loaded-namespace guard. See \code{\link[=install]{install()}}.}
}
\value{
The character vector of \code{"owner/repo"} specs passed to pak,
invisibly. Empty if nothing needed updating.
}
\description{
Installs only the members whose status is \code{"missing"} or \code{"stale"}. When
everything is current, reports that and installs nothing. Members whose
version or commit could not be checked against GitHub are reported as
unchecked rather than silently treated as current.
}
\details{
The target set is expanded over in-family dependencies (see
\code{member_deps()}) before installing, so a stale member's in-family
dependency is sent to pak alongside it even when that dependency is
already current. Without this, installing e.g. just \code{hvtiRlifetables}
sends pak to CRAN to resolve its \code{TemporalHazard} import, where the
required version may not exist.

\code{hvtiR} itself is reported but never installed. It is not a member of its
own registry, so nothing else would mention it, and it cannot be updated
from inside a running session anyway: calling \code{update()} means its namespace
is loaded, which the loaded-namespace guard refuses. When the installer is
behind, the report names \code{pak::pak("ehrlinger/hvtiR")} as the remedy.
}
\examples{
\dontrun{
update()
}
}
