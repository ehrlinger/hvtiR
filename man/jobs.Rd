% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jobs.R
\name{jobs}
\alias{jobs}
\title{The job catalog}
\usage{
jobs()
}
\value{
A data frame with one row per job type. \code{replaced_by} is a list
column of character vectors, empty where nothing replaces the row.
}
\description{
Every job type found in the studies corpus, with the package that owes it.
A job type is keyed on \code{prefix} and \code{qualifier} together, because one prefix
can carry several job types.
}
\details{
\code{disposition} says what kind of work a row is:
\itemize{
\item \code{scaffold}, genuinely repeated work; a template is the deliverable
\item \code{thin}, a template whose body is mostly calls into \code{replaced_by}
\item \code{retire}, the work is a function that exists; no template is owed
\item \code{build}, the work is a function that does not exist yet
}

\code{destination} names who owes the remaining work. \code{replaced_by} names what
already exists, as \verb{package::function}, and is frequently in a different
package from \code{destination}: a thin template lives in \code{hvtiRtemplates} and
leans on \code{hvtiPlotR}.

The taxonomy that says what a prefix \emph{means} is
\code{hvtiRutilities::hvti_taxonomy()}. This catalog says who owes it. They are
apart because validating a routing has to load the destination package, and
\code{hvtiRutilities} is imported by packages this catalog routes to.

\code{status} and \code{batch} are \code{hvtiRtemplates} scheduling fields: they answer
when \code{hvtiRtemplates} will ship a template for a row, not whether the row
is done. Both are null in the catalog for a row whose \code{destination} is
neither \code{hvtiRtemplates} nor null, because that repository has nothing of
its own to schedule there. \code{jobs()} surfaces \code{status} as \code{NA} in the
\code{status} column; it does not surface \code{batch} at all, so a caller after
that field reads the catalog row directly. The one exception to the
nulling is \code{status == "intake"}, which survives regardless of
\code{destination}: it is not a schedule value at all, but a taxonomy fact
meaning the prefix has been proposed but is not yet in
\code{hvtiRutilities::hvti_taxonomy()}.
}
\examples{
j <- jobs()
table(j$disposition)
}
